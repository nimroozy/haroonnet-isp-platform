import React from 'react';
import { Typography, Box } from '@mui/material';

function NetworkMap() {
  return (
    <Box>
      <Typography variant="h4">NetworkMap</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        NetworkMap page - Coming soon
      </Typography>
    </Box>
  );
}

export default NetworkMap;
